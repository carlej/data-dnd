{{>header}}
<main>
	{{#each camppics}}
	{{#if url}}
		<div class="photo-container">
			<div class="modal-header">
        		<button type="button" id="picture-remove-button" value={{@index}}>&times;</button>
      		</div>
			<img class="photo" src ="{{url}}">
			<p class="photo-caption">{{caption}}</p>
		</div>
	{{/if}}
	{{/each}}
</main>
{{>footer}}

{{>add-picture}}
