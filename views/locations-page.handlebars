{{>header}}
<main>
<div class="info">
	<h2>An essential skill is Survival to know where you are!</h2>
	<h3>Choose from these many locations:</3>
		<div class="select-container">
			<select id="location-select">
			<option value="" disabled selected>Select a Location</option>
			{{#each location}}
			<option value = {{idi}}>{{name}}</option>
			{{/each}}
			</select>
		</div>
</div>
</main>
{{>footer}}

{{>add-location}}
