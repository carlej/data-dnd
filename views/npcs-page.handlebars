{{>header}}
<main>
<div class="info">
	<h2>Who are you?</h2>
	<h3>There are many people to meet! Here are those currently in your story.</h3>
		<div class="select-container">
			<select id="npc-select">
			<option value="" disabled selected>Select a character</option>
			{{#each campnps}}
				<option value = {{idi}}>{{name}}</option>
			{{/each}}
			</select>
		</div>
</div>
</main>
{{>footer}}

{{>add-npc}}
